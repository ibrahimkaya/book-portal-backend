server:
  port: 8081
spring:
  main:
    banner-mode: off
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3306/jss?serverTimezone=Europe/Istanbul
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
